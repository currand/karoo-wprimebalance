<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_gauge"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_gauge"
        android:supportsRtl="true"
        android:name=".WPrimeApplication"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.currand60.wprimebalance.MAIN" />
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".extensions.WPrimeBalanceExtension"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="io.hammerhead.karooext.KAROO_EXTENSION" />
            </intent-filter>
            <meta-data
                android:name="io.hammerhead.karooext.EXTENSION_INFO"
                android:resource="@xml/extension_info" />
        </service>
        <meta-data
            android:name="io.hammerhead.karooext.MANIFEST_URL"
            android:resource="https://github.com/currand/karoo-wprimebalance/releases/latest/download/manifest.json" />
    </application>

</manifest>